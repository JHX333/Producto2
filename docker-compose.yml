version: '3.8'

services:

  laravel:
    build: ./backend
    container_name: laravel_container
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www/html

  angular:
    build: ./frontend
    container_name: angular_container
    ports:
      - "4200:80"